namespace UnionGen.InternalUtil;

public static class UnionGenInternalConst
{
    public const string DefaultConstructorWarning = "Use one of the constructors with a parameter, this (the default) one will configure the union incorrectly";
    public const string NullString = "null";
}
